# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_elastic v0.0.1
# env: dev (foundation: fixed)
# domain: doctrine-elastic.luxrnd.tech -> dev.doctrine-elastic.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_elastic_dev_internal
  web:
    external: true
    name: web
services:
  docex-smoke-elastic-dev-appdb:
    container_name: docex-smoke-elastic-dev-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-elastic-dev-appdb_data:/var/lib/postgresql/data
  docex_smoke_elastic_dev_web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: dev
    container_name: docex_smoke_elastic_dev_web
    depends_on:
      docex-smoke-elastic-dev-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-dev-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_elastic/web:0.0.1
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex_smoke_elastic_dev_web.rule=Host(`dev.doctrine-elastic.luxrnd.tech`)
      || Host(`web.dev.doctrine-elastic.luxrnd.tech`)
    - traefik.http.routers.docex_smoke_elastic_dev_web.entrypoints=websecure
    - traefik.http.routers.docex_smoke_elastic_dev_web.tls=true
    - traefik.http.routers.docex_smoke_elastic_dev_web.tls.certresolver=doctrine
    - traefik.http.services.docex_smoke_elastic_dev_web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
    volumes:
    - ./core/web/src:/service/src
    - ./core/web/dist:/service/dist
  docex_smoke_elastic_dev_worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: dev
    container_name: docex_smoke_elastic_dev_worker
    depends_on:
      docex-smoke-elastic-dev-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-dev-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_elastic/worker:0.0.1
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
    volumes:
    - ./core/worker/src:/service/src
    - ./core/worker/dist:/service/dist
volumes:
  docex-smoke-elastic-dev-appdb_data:
    name: docex-smoke-elastic-dev-appdb_data
