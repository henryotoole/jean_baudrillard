# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_elastic v0.0.1
# env: test (foundation: fixed)
# domain: doctrine-elastic.luxrnd.tech -> test.doctrine-elastic.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_elastic_test_internal
  web:
    external: true
    name: web
services:
  docex-smoke-elastic-test-db:
    container_name: docex-smoke-elastic-test-db
    environment:
      POSTGRES_DB: db
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d db
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-elastic-test-db_data:/var/lib/postgresql/data
  docex-smoke-elastic-test-web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: test
    container_name: docex-smoke-elastic-test-web
    depends_on:
      docex-smoke-elastic-test-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-test-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_elastic/web:0.0.1
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex-smoke-elastic-test-web.rule=Host(`test.doctrine-elastic.luxrnd.tech`)
      || Host(`web.test.doctrine-elastic.luxrnd.tech`)
    - traefik.http.routers.docex-smoke-elastic-test-web.entrypoints=websecure
    - traefik.http.routers.docex-smoke-elastic-test-web.tls=true
    - traefik.http.routers.docex-smoke-elastic-test-web.tls.certresolver=doctrine
    - traefik.http.services.docex-smoke-elastic-test-web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
  docex-smoke-elastic-test-worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: test
    container_name: docex-smoke-elastic-test-worker
    depends_on:
      docex-smoke-elastic-test-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-test-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_elastic/worker:0.0.1
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
volumes:
  docex-smoke-elastic-test-db_data:
    name: docex-smoke-elastic-test-db_data
