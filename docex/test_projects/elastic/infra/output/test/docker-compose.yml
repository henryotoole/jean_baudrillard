# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_elastic v0.0.4
# env: test (foundation: fixed)
# domain: doctrine-elastic.luxrnd.tech -> test.doctrine-elastic.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_elastic_test_internal
  web:
    external: true
    name: web
services:
  docex-smoke-elastic-test-appdb:
    container_name: docex-smoke-elastic-test-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-elastic-test-appdb_data:/var/lib/postgresql/data
  docex_smoke_elastic_test_web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: test
    container_name: docex_smoke_elastic_test_web
    depends_on:
      docex-smoke-elastic-test-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-test-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.4
    healthcheck:
      interval: 30s
      retries: 3
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/health
      timeout: 5s
    image: docex_smoke_elastic/web:0.0.4
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex_smoke_elastic_test_web.rule=Host(`test.doctrine-elastic.luxrnd.tech`)
      || Host(`web.test.doctrine-elastic.luxrnd.tech`)
    - traefik.http.routers.docex_smoke_elastic_test_web.entrypoints=websecure
    - traefik.http.routers.docex_smoke_elastic_test_web.tls=true
    - traefik.http.routers.docex_smoke_elastic_test_web.tls.certresolver=doctrine
    - traefik.http.services.docex_smoke_elastic_test_web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
  docex_smoke_elastic_test_worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: test
    container_name: docex_smoke_elastic_test_worker
    depends_on:
      docex-smoke-elastic-test-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-elastic-test-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.4
    image: docex_smoke_elastic/worker:0.0.4
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    tmpfs:
    - /tmp:size=25g
volumes:
  docex-smoke-elastic-test-appdb_data:
    name: docex-smoke-elastic-test-appdb_data
