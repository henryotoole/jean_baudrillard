# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.1
# env: prod (foundation: fixed)
# domain: doctrine-fixed.luxrnd.tech -> www.doctrine-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_fixed_prod_internal
  web:
    external: true
    name: web
services:
  docex-smoke-fixed-prod-db:
    container_name: docex-smoke-fixed-prod-db
    environment:
      POSTGRES_DB: db
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d db
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-prod-db_data:/var/lib/postgresql/data
  docex-smoke-fixed-prod-web:
    container_name: docex-smoke-fixed-prod-web
    depends_on:
      docex-smoke-fixed-prod-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-prod-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: registry.luxrnd.tech/docex_smoke_fixed/web:0.0.1
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex-smoke-fixed-prod-web.rule=Host(`www.doctrine-fixed.luxrnd.tech`)
      || Host(`web.www.doctrine-fixed.luxrnd.tech`)
    - traefik.http.routers.docex-smoke-fixed-prod-web.entrypoints=websecure
    - traefik.http.routers.docex-smoke-fixed-prod-web.tls=true
    - traefik.http.routers.docex-smoke-fixed-prod-web.tls.certresolver=doctrine
    - traefik.http.services.docex-smoke-fixed-prod-web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
  docex-smoke-fixed-prod-worker:
    container_name: docex-smoke-fixed-prod-worker
    depends_on:
      docex-smoke-fixed-prod-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-prod-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: registry.luxrnd.tech/docex_smoke_fixed/worker:0.0.1
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
volumes:
  docex-smoke-fixed-prod-db_data:
    name: docex-smoke-fixed-prod-db_data
