# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.1
# env: dev (foundation: fixed)
# domain: doctrine-fixed.luxrnd.tech -> dev.doctrine-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_fixed_dev_internal
  web:
    name: docex_smoke_fixed_dev_web
services:
  docex-smoke-fixed-dev-db:
    container_name: docex-smoke-fixed-dev-db
    environment:
      POSTGRES_DB: db
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d db
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-dev-db_data:/var/lib/postgresql/data
  docex-smoke-fixed-dev-web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: dev
    container_name: docex-smoke-fixed-dev-web
    depends_on:
      docex-smoke-fixed-dev-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-dev-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_fixed/web:0.0.1
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex-smoke-fixed-dev-web.rule=Host(`dev.doctrine-fixed.luxrnd.tech`)
      || Host(`web.dev.doctrine-fixed.luxrnd.tech`)
    - traefik.http.routers.docex-smoke-fixed-dev-web.entrypoints=websecure
    - traefik.http.routers.docex-smoke-fixed-dev-web.tls=true
    - traefik.http.services.docex-smoke-fixed-dev-web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
    volumes:
    - ./core/web/src:/service/src
    - ./core/web/dist:/service/dist
  docex-smoke-fixed-dev-worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: dev
    container_name: docex-smoke-fixed-dev-worker
    depends_on:
      docex-smoke-fixed-dev-db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-dev-db
      DATABASE_NAME: db
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_USER: ${POSTGRES_USER}
    image: docex_smoke_fixed/worker:0.0.1
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - ./core/worker/src:/service/src
    - ./core/worker/dist:/service/dist
volumes:
  docex-smoke-fixed-dev-db_data:
    name: docex-smoke-fixed-dev-db_data
