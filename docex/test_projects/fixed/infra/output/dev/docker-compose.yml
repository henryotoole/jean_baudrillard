# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.3
# env: dev (foundation: fixed)
# domain: doctrine-fixed.luxrnd.tech -> dev.doctrine-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_fixed_dev_internal
  web:
    external: true
    name: web
services:
  docex-smoke-fixed-dev-appdb:
    container_name: docex-smoke-fixed-dev-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-dev-appdb_data:/var/lib/postgresql/data
  docex_smoke_fixed_dev_events:
    container_name: docex_smoke_fixed_dev_events
    environment:
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: '1'
    image: clickhouse/clickhouse-server:24.10
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex_smoke_fixed_dev_events_data:/var/lib/clickhouse
  docex_smoke_fixed_dev_probe:
    container_name: docex_smoke_fixed_dev_probe
    image: nginx:1.27-alpine
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
  docex_smoke_fixed_dev_web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: dev
    container_name: docex_smoke_fixed_dev_web
    depends_on:
      docex-smoke-fixed-dev-appdb:
        condition: service_healthy
      docex_smoke_fixed_dev_events:
        condition: service_started
      docex_smoke_fixed_dev_probe:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      CLICKHOUSE_HOST: docex_smoke_fixed_dev_events
      CLICKHOUSE_PORT: '9000'
      DATABASE_HOST: docex-smoke-fixed-dev-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.3
      SIDECAR_HOST: docex_smoke_fixed_dev_probe
      SIDECAR_PORT: '80'
    image: docex_smoke_fixed/web:0.0.3
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex_smoke_fixed_dev_web.rule=Host(`dev.doctrine-fixed.luxrnd.tech`)
      || Host(`web.dev.doctrine-fixed.luxrnd.tech`)
    - traefik.http.routers.docex_smoke_fixed_dev_web.entrypoints=websecure
    - traefik.http.routers.docex_smoke_fixed_dev_web.tls=true
    - traefik.http.routers.docex_smoke_fixed_dev_web.tls.certresolver=doctrine
    - traefik.http.services.docex_smoke_fixed_dev_web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
    volumes:
    - ./core/web/src:/service/src
    - ./core/web/dist:/service/dist
  docex_smoke_fixed_dev_worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: dev
    container_name: docex_smoke_fixed_dev_worker
    depends_on:
      docex-smoke-fixed-dev-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-dev-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.3
    image: docex_smoke_fixed/worker:0.0.3
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - ./core/worker/src:/service/src
    - ./core/worker/dist:/service/dist
volumes:
  docex-smoke-fixed-dev-appdb_data:
    name: docex-smoke-fixed-dev-appdb_data
  docex_smoke_fixed_dev_events_data:
    name: docex_smoke_fixed_dev_events_data
