# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.3
# env: test (foundation: fixed)
# domain: doctrine-fixed.luxrnd.tech -> test.doctrine-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_fixed_test_internal
  web:
    external: true
    name: web
services:
  docex-smoke-fixed-test-appdb:
    container_name: docex-smoke-fixed-test-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-test-appdb_data:/var/lib/postgresql/data
  docex_smoke_fixed_test_events:
    container_name: docex_smoke_fixed_test_events
    environment:
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: '1'
    image: clickhouse/clickhouse-server:24.10
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex_smoke_fixed_test_events_data:/var/lib/clickhouse
  docex_smoke_fixed_test_probe:
    container_name: docex_smoke_fixed_test_probe
    image: nginx:1.27-alpine
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
  docex_smoke_fixed_test_web:
    build:
      context: ./core/web
      dockerfile: Dockerfile
      target: test
    container_name: docex_smoke_fixed_test_web
    depends_on:
      docex-smoke-fixed-test-appdb:
        condition: service_healthy
      docex_smoke_fixed_test_events:
        condition: service_started
      docex_smoke_fixed_test_probe:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      CLICKHOUSE_HOST: docex_smoke_fixed_test_events
      CLICKHOUSE_PORT: '9000'
      DATABASE_HOST: docex-smoke-fixed-test-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.3
      SIDECAR_HOST: docex_smoke_fixed_test_probe
      SIDECAR_PORT: '80'
    image: docex_smoke_fixed/web:0.0.3
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex_smoke_fixed_test_web.rule=Host(`test.doctrine-fixed.luxrnd.tech`)
      || Host(`web.test.doctrine-fixed.luxrnd.tech`)
    - traefik.http.routers.docex_smoke_fixed_test_web.entrypoints=websecure
    - traefik.http.routers.docex_smoke_fixed_test_web.tls=true
    - traefik.http.routers.docex_smoke_fixed_test_web.tls.certresolver=doctrine
    - traefik.http.services.docex_smoke_fixed_test_web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
  docex_smoke_fixed_test_worker:
    build:
      context: ./core/worker
      dockerfile: Dockerfile
      target: test
    container_name: docex_smoke_fixed_test_worker
    depends_on:
      docex-smoke-fixed-test-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-test-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.3
    image: docex_smoke_fixed/worker:0.0.3
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
volumes:
  docex-smoke-fixed-test-appdb_data:
    name: docex-smoke-fixed-test-appdb_data
  docex_smoke_fixed_test_events_data:
    name: docex_smoke_fixed_test_events_data
