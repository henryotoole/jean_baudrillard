---
# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.21
# env: stage
- name: Deploy docex_smoke_fixed stage
  hosts: stage
  become: true
  vars:
    project: "docex_smoke_fixed"
    env: "stage"
    version: "0.0.21"
    deploy_root: "/opt/docex_smoke_fixed/stage"
  tasks:
    - name: Ensure deploy directory exists
      ansible.builtin.file:
        path: "{{ deploy_root }}"
        state: directory
        mode: "0750"

    - name: Render docker-compose.yml onto host
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/../stage/docker-compose.yml"
        dest: "{{ deploy_root }}/docker-compose.yml"
        mode: "0640"

    - name: Render TTE store onto host
      ansible.builtin.copy:
        src: "{{ tte_store_file }}"
        dest: "{{ deploy_root }}/tte.env"
        mode: "0600"
      # Skipped in dry-run (--check passes no extra-vars); the real release
      # always supplies tte_store_file. Mod 093.
      when: tte_store_file is defined

    - name: Render .env onto host
      ansible.builtin.copy:
        src: "{{ agg_env_file }}"
        dest: "{{ deploy_root }}/.env"
        mode: "0600"
      # Skipped in dry-run (--check passes no extra-vars); the real release
      # always supplies agg_env_file. Mod 093.
      when: agg_env_file is defined

    - name: Pull all images
      community.docker.docker_compose_v2_pull:
        project_src: "{{ deploy_root }}"
        project_name: "docex-smoke-fixed-stage"
        policy: always

    - name: Run migrations for api
      ansible.builtin.command:
        cmd: docker compose -p docex-smoke-fixed-stage run --rm docex-smoke-fixed-stage-api-exec /service/migrate.sh
        chdir: "{{ deploy_root }}"
      register: api_migrate_result
      changed_when: true
      tags:
        - migrate

    - name: Bring up the stack
      community.docker.docker_compose_v2:
        project_src: "{{ deploy_root }}"
        project_name: "docex-smoke-fixed-stage"
        state: present
        remove_orphans: true
