# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.2
# env: stage (foundation: fixed)
# domain: doctrine-fixed.luxrnd.tech -> stage.doctrine-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
networks:
  internal:
    internal: true
    name: docex_smoke_fixed_stage_internal
  web:
    external: true
    name: web
services:
  docex-smoke-fixed-stage-appdb:
    container_name: docex-smoke-fixed-stage-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-stage-appdb_data:/var/lib/postgresql/data
  docex_smoke_fixed_stage_web:
    container_name: docex_smoke_fixed_stage_web
    depends_on:
      docex-smoke-fixed-stage-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-stage-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.2
    image: registry.luxrnd.tech/docex_smoke_fixed/web:0.0.2
    labels:
    - traefik.enable=true
    - traefik.http.routers.docex_smoke_fixed_stage_web.rule=Host(`stage.doctrine-fixed.luxrnd.tech`)
      || Host(`web.stage.doctrine-fixed.luxrnd.tech`)
    - traefik.http.routers.docex_smoke_fixed_stage_web.entrypoints=websecure
    - traefik.http.routers.docex_smoke_fixed_stage_web.tls=true
    - traefik.http.routers.docex_smoke_fixed_stage_web.tls.certresolver=doctrine
    - traefik.http.services.docex_smoke_fixed_stage_web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
  docex_smoke_fixed_stage_worker:
    container_name: docex_smoke_fixed_stage_worker
    depends_on:
      docex-smoke-fixed-stage-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-stage-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      PROJECT_VERSION: 0.0.2
    image: registry.luxrnd.tech/docex_smoke_fixed/worker:0.0.2
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
volumes:
  docex-smoke-fixed-stage-appdb_data:
    name: docex-smoke-fixed-stage-appdb_data
