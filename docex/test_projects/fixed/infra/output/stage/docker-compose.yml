# Generated by `docex compile`. Do not edit by hand.
# project: docex_smoke_fixed v0.0.4
# env: stage (foundation: fixed)
# apex_domain: luxrnd.tech -> stage.docex-smoke-fixed.luxrnd.tech
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
configs:
  otelcol_config:
    content: "# Generated by `docex compile`. Do not edit by hand.\n# env: stage\n\
      receivers:\n  otlp:\n    protocols:\n      http:\n        endpoint: 127.0.0.1:4318\n\
      processors:\n  batch: {}\nexporters:\n  otlphttp:\n    endpoint: $${env:OBSERVABILITY_BACKEND_URL}\n\
      \    headers:\n      authorization: $${env:TELEMETRY_API_KEY}\nextensions:\n\
      \  health_check:\n    endpoint: 127.0.0.1:13133\nservice:\n  extensions: [health_check]\n\
      \  pipelines:\n    traces:  { receivers: [otlp], processors: [batch], exporters:\
      \ [otlphttp] }\n    metrics: { receivers: [otlp], processors: [batch], exporters:\
      \ [otlphttp] }\n    logs:    { receivers: [otlp], processors: [batch], exporters:\
      \ [otlphttp] }\n"
networks:
  internal:
    internal: true
    name: docex-smoke-fixed-stage-internal
  web:
    external: true
    name: docex-smoke-fixed-stage-web
services:
  docex-smoke-fixed-stage-appdb:
    container_name: docex-smoke-fixed-stage-appdb
    environment:
      POSTGRES_DB: appdb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER} -d appdb
      timeout: 5s
    image: postgres:15
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-stage-appdb_data:/var/lib/postgresql/data
  docex-smoke-fixed-stage-events:
    container_name: docex-smoke-fixed-stage-events
    environment:
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: '1'
    image: clickhouse/clickhouse-server:24.10
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
    volumes:
    - docex-smoke-fixed-stage-events_data:/var/lib/clickhouse
  docex-smoke-fixed-stage-probe:
    container_name: docex-smoke-fixed-stage-probe
    image: nginx:1.27-alpine
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
  docex-smoke-fixed-stage-web:
    container_name: docex-smoke-fixed-stage-web
    depends_on:
      docex-smoke-fixed-stage-appdb:
        condition: service_healthy
      docex-smoke-fixed-stage-events:
        condition: service_started
      docex-smoke-fixed-stage-probe:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      CLICKHOUSE_HOST: docex-smoke-fixed-stage-events
      CLICKHOUSE_PORT: '9000'
      DATABASE_HOST: docex-smoke-fixed-stage-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://localhost:4318
      OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=docex_smoke_fixed,service.version=0.0.4,deployment.environment.name=stage
      OTEL_SERVICE_NAME: web
      PROJECT_VERSION: 0.0.4
      SIDECAR_HOST: docex-smoke-fixed-stage-probe
      SIDECAR_PORT: '80'
    healthcheck:
      interval: 30s
      retries: 3
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/health
      timeout: 5s
    image: registry.luxrnd.tech/docex_smoke_fixed/web:0.0.4
    labels:
    - traefik.enable=true
    - traefik.docker.network=docex-smoke-fixed-stage-web
    - traefik.http.routers.docex-smoke-fixed-stage-web.rule=Host(`web.stage.docex-smoke-fixed.luxrnd.tech`)
      || Host(`stage.docex-smoke-fixed.luxrnd.tech`)
    - traefik.http.routers.docex-smoke-fixed-stage-web.entrypoints=websecure
    - traefik.http.routers.docex-smoke-fixed-stage-web.tls=true
    - traefik.http.routers.docex-smoke-fixed-stage-web.tls.certresolver=doctrine
    - traefik.http.services.docex-smoke-fixed-stage-web.loadbalancer.server.port=8080
    logging: *default-logging
    networks:
    - web
    - internal
    restart: unless-stopped
  docex-smoke-fixed-stage-web-otelcol:
    command:
    - --config=/etc/otelcol/config.yaml
    configs:
    - source: otelcol_config
      target: /etc/otelcol/config.yaml
    container_name: docex-smoke-fixed-stage-web-otelcol
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 128M
    environment:
      OBSERVABILITY_BACKEND_URL: https://hyperdx.luxrnd.tech
      TELEMETRY_API_KEY: ${TELEMETRY_API_KEY:-}
    image: otel/opentelemetry-collector:0.153.0@sha256:74edb825a429b415262e7eb7a99ed77685c9b2b7238ef69fb42a3625df75458f
    logging: *default-logging
    network_mode: service:docex-smoke-fixed-stage-web
    restart: unless-stopped
  docex-smoke-fixed-stage-worker:
    container_name: docex-smoke-fixed-stage-worker
    depends_on:
      docex-smoke-fixed-stage-appdb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 512MB
    environment:
      DATABASE_HOST: docex-smoke-fixed-stage-appdb
      DATABASE_NAME: appdb
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_PORT: '5432'
      DATABASE_SSLMODE: disable
      DATABASE_USER: ${POSTGRES_USER}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://localhost:4318
      OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=docex_smoke_fixed,service.version=0.0.4,deployment.environment.name=stage
      OTEL_SERVICE_NAME: worker
      PROJECT_VERSION: 0.0.4
    image: registry.luxrnd.tech/docex_smoke_fixed/worker:0.0.4
    logging: *default-logging
    networks:
    - internal
    restart: unless-stopped
  docex-smoke-fixed-stage-worker-otelcol:
    command:
    - --config=/etc/otelcol/config.yaml
    configs:
    - source: otelcol_config
      target: /etc/otelcol/config.yaml
    container_name: docex-smoke-fixed-stage-worker-otelcol
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 128M
    environment:
      OBSERVABILITY_BACKEND_URL: https://hyperdx.luxrnd.tech
      TELEMETRY_API_KEY: ${TELEMETRY_API_KEY:-}
    image: otel/opentelemetry-collector:0.153.0@sha256:74edb825a429b415262e7eb7a99ed77685c9b2b7238ef69fb42a3625df75458f
    logging: *default-logging
    network_mode: service:docex-smoke-fixed-stage-worker
    restart: unless-stopped
volumes:
  docex-smoke-fixed-stage-appdb_data:
    name: docex-smoke-fixed-stage-appdb_data
  docex-smoke-fixed-stage-events_data:
    name: docex-smoke-fixed-stage-events_data
