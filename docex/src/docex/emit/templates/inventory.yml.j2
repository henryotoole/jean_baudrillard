# Generated by `docex compile`. Do not edit by hand.
# Inventory for {{ project }} {{ env }} (fixed foundation).
# Host derives from project's `domain:` per release_mechanism.md § Inventory.
all:
  children:
    {{ env }}:
      hosts:
        {{ subdomain }}:
          ansible_user: deploy
          ansible_ssh_private_key_file: "{{ '{{' }} playbook_dir {{ '}}' }}/../../deploy_creds/{{ env }}"
          ansible_python_interpreter: /usr/bin/python3
