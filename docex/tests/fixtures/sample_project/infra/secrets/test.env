# Placeholder secrets for the sample fixture's test env.
# Real projects gitignore <env>.env; committed here for fixture use.
#
# Engine-managed env vars (POSTGRES_PASSWORD minted, POSTGRES_USER fixed)
# are NOT declared here — they'd collide with the TTE store / inlined
# values during aggregation.
