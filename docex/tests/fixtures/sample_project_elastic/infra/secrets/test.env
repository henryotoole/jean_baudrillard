# Placeholder secrets for the elastic fixture's test env.
# Engine-managed env vars (POSTGRES_PASSWORD minted, POSTGRES_USER fixed) are
# NOT declared here — they'd collide with the TTE store during aggregation.
