# Placeholder secrets for the elastic fixture's dev env.
# dev/test always run on the fixed foundation (per the doctrine), so
# these values are consumed by docker-compose, not SSM.
POSTGRES_USER=sample
POSTGRES_PASSWORD=sample-dev-password
POSTGRES_DB=sample
