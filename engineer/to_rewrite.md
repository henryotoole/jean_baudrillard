# To Rewrite

Segments of the core doctrine to re-write later, when I have time

## docex.md

Command procedures for:
`./bin/docex docs linkmap <depth>`
`./bin/docex docs overhead <file>`
`./bin/docex docs changed <git_ref>`
`./bin/docex docs cxt_groups <tokens_max> {<git_ref> | all}`

All written by machine, non-edited.

## adrs.md

Re-write after current mod.